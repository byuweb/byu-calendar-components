@import '../shared-styles/colors';
@import '../shared-styles/fonts';
@import '../shared-styles/mixins';

@mixin byuCalendarMinimalTile() {
  margin-bottom: 15px;
  display: flex;
  flex-direction: row;
}

@mixin minimalSectionFirst() {
  background-color: $royal;
  color: $white;
  height: 120px;
  width: 200px;
  margin-right: 10px;
  text-align: center;
}

@mixin minimalSectionSecond() {
  background-color: $medlightgray;
  width: 100%;
  padding: 10px;
}

@mixin minimalMonthAndDay() {
  font-size: 36px;
  font-family: $vitesse;
  padding: 10px;
}

@mixin minimalTime() {
  font-size: 20px;
  font-family: $vitesse;
}

@mixin minimalTitle() {
  font-size: 24px;
  margin-bottom: 10px;
}

@mixin minimalLocation() {
  font-size: 12px;
  text-transform: uppercase;
  font-weight: bold;
  color: $darkgray;
}

// Mixins for the extra file

@mixin minimalTitleOverride() {
  color: $navy;
  font-weight: 500;
}

@mixin minimalTitleOverrideHover() {
  color: $royal;
}